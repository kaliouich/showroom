# Renseigner l'IP sortie par Terraform : terraform -chdir=../terraform output -raw public_ip
# En local sur le noeud, ansible_connection=local evite de passer par SSH.
[showroom]
k3s-server ansible_host=CHANGEME ansible_user=ubuntu

[showroom:vars]
ansible_python_interpreter=/usr/bin/python3
